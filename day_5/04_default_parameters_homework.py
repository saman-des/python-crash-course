# ---------------------------------------------------------
# HOMEWORK 4: DEFAULT PARAMETERS 🎯
# ---------------------------------------------------------


# TASK 1: Greeting with Default
# Define a function called 'welcome' with two parameters:
#   name (required) and greeting (default "Hello").
# It should print: "[greeting], [name]!"
# Test it with:
#   welcome("Sam") → "Hello, Sam!"
#   welcome("Sam", "Hi") → "Hi, Sam!"
#   welcome("Sam", "Good morning") → "Good morning, Sam!"
# Write your code below:





# TASK 2: Power Function
# Define a function called 'power' with two parameters:
#   base (required) and exponent (default 2).
# Return base raised to the exponent power.
# Test it with:
#   power(3) → 9 (3²)
#   power(2, 3) → 8 (2³)
#   power(5, 0) → 1 (5⁰)
# Write your code below:





# TASK 3: Order Summary
# Define a function called 'order_summary' with:
#   item_name (required), quantity (default 1), price_each (default 10.0)
# Return a string: "[quantity] x [item_name] = $[total]"
# Test it with:
#   order_summary("pencil") → "1 x pencil = $10.0"
#   order_summary("notebook", 3) → "3 x notebook = $30.0"
#   order_summary("pen", 5, 2.5) → "5 x pen = $12.5"
# Write your code below:





# TASK 4: Safe Divider
# Define a function called 'safe_divide' with two parameters:
#   numerator and denominator (default 1).
# If denominator is 0, return "Cannot divide by zero!"
# Otherwise return numerator / denominator.
# Test it with (10, 2), (5, 0), and (7,).
# Write your code below:





# ⭐ BONUS CHALLENGE:
# Define a function called 'create_profile' with parameters:
#   username (required), status (default "active"), role (default "user").
# Return a dictionary with these keys and values.
# Test it with various combinations of arguments.
# Write your code below:

